name: pantry_logic
description: "Pantry Logic — shared household food and meal decision app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  # Supabase backend
  supabase_flutter: ^2.8.4

  # Load .env credentials
  flutter_dotenv: ^5.2.1

  # Nunito Sans typography
  google_fonts: ^6.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env

  fonts:
    - family: NotoColorEmoji
      fonts:
        - asset: fonts/NotoColorEmoji-Regular.ttf
